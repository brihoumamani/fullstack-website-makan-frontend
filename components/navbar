import Link from 'next/link';

export default function Navbar() {
  return (
    <nav className="w-full bg-white shadow-sm sticky top-0 z-50">
      <div className="max-w-[1400px] mx-auto px-6 py-4 flex justify-between items-center">
        
        {/* Logo Section */}
        <div className="flex items-center gap-3 flex-shrink-0">
          <svg className="w-8 h-8 text-brandGreen" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 3L2 12h3v8h6v-6h2v6h6v-8h3L12 3z" />
          </svg>
          <Link href="/" className="text-2xl font-bold text-gray-900 tracking-tight">
            fasthome
          </Link>
        </div>

        {/* Center Navigation Links */}
        <div className="hidden md:flex space-x-8 text-sm font-medium text-gray-700">
          <Link href="/properties" className="hover:text-brandRed transition-colors">For Sale</Link>
          <Link href="/rentals" className="hover:text-brandRed transition-colors">For Rent</Link>
          <Link href="/daily" className="hover:text-brandRed transition-colors">Daily Rental</Link>
          <Link href="/projects" className="hover:text-brandRed transition-colors">Projects</Link>
        </div>

        {/* Right Actions */}
        <div className="flex items-center gap-4">
          <button className="bg-brandRed hover:bg-red-700 text-white px-6 py-2 rounded text-sm font-medium transition-colors">
            Advertise
          </button>
          
          {/* User Profile Icon */}
          <button className="w-9 h-9 rounded-full bg-gray-900 text-white flex items-center justify-center">
            <svg className="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path fillRule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clipRule="evenodd" />
            </svg>
          </button>
        </div>

      </div>
    </nav>
  );
}